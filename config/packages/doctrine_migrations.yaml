# https://symfony.com/bundles/DoctrineMigrationsBundle/current/index.html#configuration
doctrine_migrations:
    migrations_paths:
        # namespace is arbitrary but should be different from App\Migrations
        # as migrations classes should NOT be autoloaded
        'DoctrineMigrations\Schema': '%kernel.project_dir%/migrations/schemas'
    enable_profiler: false
